import { DESIGN_TOKENS } from '@ledova/shared-constants';

/**
 * Static layout constants. These don't change between themes.
 * For theme-aware values, use useAppTheme() from contexts.
 */
export const layout = DESIGN_TOKENS.layout;
